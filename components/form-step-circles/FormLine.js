import { Box } from "@mui/material";

export const FormLine = ({ colour }) => (
    <Box sx={{ 
        margin: "0.15vw", 
        justifyContent: "center", 
        backgroundColor: colour, 
        width: "6em", 
        height: "0.15em" }} />
)
