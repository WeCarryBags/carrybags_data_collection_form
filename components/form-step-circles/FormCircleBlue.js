import { Avatar } from "@mui/material";

export const FormCircleBlue = ({ text }) => (
    <Avatar
        children={text}
        sx={{
            backgroundColor: '#3E8EF1',
            margin: "0.1em"
        }} />
)
